{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-shell",
  "description": "TASK-903 exposes only explicit app commands and grants no shell, filesystem, process, dialog, or network plugin permissions.",
  "local": true,
  "windows": ["main"],
  "permissions": []
}
